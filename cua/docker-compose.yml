services:
  data-fetcher:
    build: .
    env_file:
      - ../.env
    ports:
      - "5900:5900"   # VNC
      - "6080:6080"   # noVNC — watch in browser at http://localhost:6080
    volumes:
      - ./downloads:/home/computeruse/Downloads
